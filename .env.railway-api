# Environment Variables for Railway a2a-api Service (API/Health/Metrics)
# Copy these into Railway Dashboard → a2a-api → Variables

NODE_ENV=production
LOG_LEVEL=info
ENABLE_STREAMING=false
STREAM_HOST=0.0.0.0
STREAM_PORT=0
MAX_CONCURRENCY=100
MAX_QUEUE_SIZE=50000
REQUEST_TTL_MS=300000
IDEMP_TTL_MS=900000

# IMPORTANT: Use the SAME token as a2a-ws service
STREAM_TOKEN=SAME_AS_WS_SERVICE

# Your Ollama or LLM service URL (same as ws service)
LOCAL_LLM_URL=https://your-ollama-host.example.com

# Note: METRICS_PORT is automatically set by Railway as $PORT
# Do not set METRICS_PORT manually - Railway handles it
