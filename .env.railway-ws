# Environment Variables for Railway a2a-ws Service (WebSocket)
# Copy these into Railway Dashboard → a2a-ws → Variables

NODE_ENV=production
LOG_LEVEL=info
ENABLE_STREAMING=true
STREAM_HOST=0.0.0.0
METRICS_PORT=0
MAX_CONCURRENCY=100
MAX_QUEUE_SIZE=50000
MAX_SUBS_PER_REQUEST=16
REQUEST_TTL_MS=300000
IDEMP_TTL_MS=900000

# IMPORTANT: Set these values
# Generate secure token: openssl rand -hex 32
STREAM_TOKEN=REPLACE_WITH_YOUR_SECURE_TOKEN

# Your Ollama or LLM service URL
LOCAL_LLM_URL=https://your-ollama-host.example.com

# Note: STREAM_PORT is automatically set by Railway as $PORT
# Do not set STREAM_PORT manually - Railway handles it
